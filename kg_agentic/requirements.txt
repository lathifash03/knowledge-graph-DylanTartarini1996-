# Core runtime for Phases 1-4 (Neo4j migration, validity, trust, verifier).
# These install cleanly on Python 3.9+.
neo4j>=5.20.0
python-dotenv>=1.0.0
groq>=0.11.0

# Offline RAGAS evaluation is OPTIONAL and heavier (may need Python 3.10+).
# evaluation.py is import-guarded, so the stack runs without these.
# Install separately when you want RAGAS metrics:
#     python3 -m pip install -r kg_agentic/requirements-eval.txt
